[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"

# App routes fall back to the SPA shell; "/" serves the static landing page
# (Netlify only applies this rewrite when no file matches the path).
[[redirects]]
  from = "/*"
  to = "/app.html"
  status = 200
